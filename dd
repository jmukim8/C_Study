#include <stdio.h>

int main(void) 
{
  int i,j,q;

  for(i = 1; i <= 5; i++)
  {
    for(q = 4; q > 0; q--)
    {
      printf(" ");
    }    
    for(j = i; j >= 1; j--)
    {
      printf("%d", j);
    }
    printf("\n");
  }
  return 0;
}
